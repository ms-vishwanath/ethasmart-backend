/**
 * EthaSmart frontend config – edit this file only to change API and app settings.
 */
window.ETHASMART_CONFIG = {
  // Backend API base URL (no trailing slash)
  API_BASE: "http://127.0.0.1:7021",

  // App name (used in titles / UI if needed)
  APP_NAME: "ETHASMART",

  // Polling intervals (milliseconds)
  DASHBOARD_POLL_MS: 3000,
  ENERGY_POLL_MS: 1000,
  MAINTENANCE_POLL_MS: 5000,
  INSIGHTS_POLL_MS: 1000,
};
